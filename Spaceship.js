function Spaceship (input)
{
    let width = Number(input[0]);
    let length = Number(input[1]);
    let height = Number(input[2]);
    let astronautNumbers = Number;

    let Nums = (width * height) / length;

    if (astronautNumbers === 3 && 10)
    {
        console.log(`The spacecraft holds ${astronautNumber.toFixed(2)} astronauts.`)
    
        if (astronautNumber >= 3)
        {
            console.log('The spacecraft is too small.')
            {
                if (astronautNumber <= 10)
                {
                    console.log('The spacecraft is too big.')
                }
            }
        }
    }
}

Spaceship([3.5, 4, 5, 1.70]);